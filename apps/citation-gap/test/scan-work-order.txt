# SEO AND AI-VISIBILITY WORK ORDER

## NO CHANGE SINCE LAST SCAN (September 8, 2026 at 3:39 AM UTC)
Page fingerprint identical — sha256 e6eda8c5…
Competitor medians reused from that scan (age: 0 hours). No SERP credits spent.
Open tasks: 12 (was 13). Rank 20 · Answer 15.
The page was re-fetched and re-rendered on this scan; only the Google results and competitor pages are reused.
Run a full rescan: tick “Force a full rescan” in the scan form.

CONTEXT
Page to edit ....... http://127.0.0.1:41755/site/
Target keyword ..... "telematics"
Brand .............. 127
Market ............. US / EN
Measured against ... the 3 pages currently ranking for this keyword
Audit date ......... September 8, 2026 at 3:39 AM UTC
Rank Score ......... 20/100   (how closely this page matches the pages beating it; stable across competitor samples)
Answer Score ....... 15/100   (whether AI answer engines can quote it; stable across competitor samples)

## METHODOLOGY
Every figure in this work order is reproducible from these rules. Check the method before the
numbers; a number without its method cannot be verified.

  Fetch mode ......... served HTML + rendered DOM (headless Chromium, computed styles read)
  Fetched URL ........ exactly as given, no cache-busting parameter added; freshness requested
                       via Cache-Control: no-cache header
  Render wait ........ 2,500 ms after networkidle2, scrolled to bottom: yes (4 steps), then 1,250 ms more
  Render input ....... mouse move + wheel scroll (4 steps down, then back up) + key press; prefers-reduced-motion: no-preference; viewport 1366×900
  Gate self-check .... 2 markers at first paint → 1/2 released, 1 still hidden (“Get a Free Demo”)
                       PARTIALLY RELEASED — the 1 still hidden are real residuals, listed as findings
                       Method: every marked element tagged at first paint and re-read by identity after a down pass, a reverse pass and a settle, regardless of scroll position; markers inserted after first paint are tagged and checked separately
                       still hidden: [call to action] “Get a Free Demo” — visibility:hidden, marker class still present
  Word count rule .... Source: the HTML as the server sent it (no JavaScript run, no DOMParser innerText). Section
                       boundary: the first <main> or <article> whose text exceeds 500 characters, otherwise <body>
                       with <nav>, <header>, <footer>, <aside> and <form> removed. <script>, <style>, <noscript>,
                       <template>, <svg>, comments, and any element with a hidden attribute or an inline
                       display:none / visibility:hidden are dropped with their contents (a stylesheet rule cannot
                       be resolved without rendering; see the rendered count). Carousel clone slides
                       (swiper-slide-duplicate, slick-cloned, owl-item cloned, splide__slide--clone) are dropped,
                       since a loop carousel repeats every slide and would count each testimonial twice. Every tag
                       is removed, so alt text, aria-labels, title attributes and other attribute values are NOT
                       counted; HTML entities are decoded; whitespace is collapsed. The text is split on whitespace
                       and a token counts as a word only if it contains at least one letter or digit (so a lone
                       dash or pipe is not a word). Hyphenated compounds are one word; numerals are words.
                       Region used on this page: <body> minus nav/header/footer/aside/form
  Text fingerprint ... sha256 e6eda8c5416a326bc69f459fa4f61bd301086591e91f67af8082891426d83d40
                       The stripped text is downloadable from the report ("Download counted text");
                       apply the rule above to it and you get exactly 72.
  Block definition ... A block is the OUTERMOST element carrying an entrance-animation marker (class
                       elementor-invisible, wpb_animate_when_almost_visible, wow, animate__animated, or a data-aos
                       attribute) that contains text. A gated element nested inside another gated element is
                       counted within its parent, not separately. The raw element count is every element carrying a
                       marker, nested or not.
  Measurement basis .. SERVED HTML for every page in the comparison (this page and all 3 competitors). The rendered figure for this page is printed beside it and never mixed into the median.
                       One basis per comparison is asserted at scan time; a page that could only be measured on the other basis is
                       excluded with the disposition BASIS_MISMATCH rather than averaged in. Tick “Render every competitor” for the rendered basis.
  Word count check ... BASIS DIFFERENCE 33% — served 72 · rendered 48 (48 painted + 0 reachable).
                       Two quantities, not one unreliable number. The difference is: 1 carousel clone slides (13 words) excluded on both sides; 24 words served but not painted
                       or reachable (script-gated, display:none, or inside a hidden template wrapper).
                       SCORED FIGURE: served (72) — the basis every page in this comparison shares. Any task derived from
                       the word count is capped at HIGH while this difference stands (P14).
                       A = served HTML, <body> minus nav/header/footer/aside/form, regex parser · B = rendered page, same boundary (<body> minus nav/header/footer/aside/form), painted text (one tree walk; every text node whose ancestors all pass display / visibility / opacity / hidden / client-rect / clip tests) plus REACHABLE text (off-stage carousel slides and closed tab panels with a painted sibling; clones excluded)
                       for comparison only: innerText 74 · textContent 136 · rendered DOM under the served rules 85
                       whole page (served HTML) 79 · whole page (rendered, painted) 55
  Painted words, <body> minus nav/header/footer/aside/form ...... 48 total (measured at settle, 7.5 s after navigation, 2026-09-08T03:39:16.316Z)
                               35 body copy
                               13 carousel (0 painted slide(s); 0 more reachable = 0 words; 1 clones excluded)
                               0 feed / loop grid
                               0 other (nav, header, footer, form inside the region)
                               composition sums to the total ✓
  Reachable, not painted ..... 0 words in 0 carousel slide(s) one click away, in the DOM a crawler parses (P11)
  Rendered figure ............ 48 = 48 painted + 0 reachable
  First paint, no input ...... painted 43 · innerText 69 (1.0 s after navigation, before any scroll) — what a hand count at the top of a fresh page sees; entrance animations below the fold have not fired yet
  Carousel accounting ........ 2 items in 1 container(s) = 1 clones + 0 painted + 0 reachable + 1 excluded, all classified
    .swiper (carousel).......................................... 2 items = 1 clones + 0 painted (0w) + 0 reachable (0w) + 1 excluded (1 positioned off-page)
  Carousel clones .... 1 matched by `.swiper-slide-duplicate` (13 words) in served HTML
                       0 matched by generic fallback
                       rendered: 1 × .swiper-slide-duplicate — 1 painted element(s), 1 with painted text, 13 of 13 words painted (innerText 13)
                       rendered: 1 × .swiper-slide:not(.swiper-slide-duplicate) — 0 painted element(s), 0 with painted text, 0 of 12 words painted (innerText 12)
  Competitor sample .. 10 requested → 3 used, 7 not used:
                       SERP_SHORT ×4         (google) — Google returned only 6 organic results for this query; 4 requested slots had no page to fill
                       BELOW_MIN_CONTENT     thin.com — 72 main-content words, 2 headings — below the 300-word / 1-heading floor; likely a script-gated or blocked fetch; rendered in a browser too: 81 words, 2 headings — still below the floor
                       BASIS_MISMATCH        jsbuilt.com — served HTML gave 1 words (script-gated); rendered in a browser it reads 527 words, 2 headings — a different measurement basis from the other competitors, so it is NOT used on the served basis. Tick “Render every competitor” to use it.
                       FAILED                blocked.com — HTTP 404
                       arithmetic closes: used + dispositions = requested
                       RENDERED  jsbuilt.com — served HTML gave 1 words (script-gated); rendered in a browser: 527 words, 2 headings. NOT USED on the served basis (BASIS_MISMATCH); would be used under “Render every competitor”.
                       Effort model: 1 day = 6 hours, half day = 3 hours; pts/hr figures use it.
                       Minimum-content gate: a page under 300 main-content words or with no headings is a failed or
                       script-gated fetch, not a competitor, and is excluded so it cannot drag every median down.
                       word count min/median/max 558 / 558 / 558
  Score confidence ... Rank 20–20, Answer 15–15 — leave-one-out across the 3 competitor pages.
                       A move inside that band between scans is sample noise, not progress.
  Median confidence .. each median below carries its leave-one-out range; a task whose median moves
                       when one competitor is dropped is marked SOFT TARGET and its number may change next scan.
  Heading rule ....... Every <h1>–<h6> in the served HTML is counted, including one inside a container the HTML
                       itself hides (hidden attribute, inline display:none / visibility:hidden, carousel clone) —
                       such a heading is labelled HIDDEN, never dropped, so the count matches the page source. A
                       heading inside a post-feed, loop, carousel or <article> wrapper is TEMPLATE (generated by a
                       widget; cannot be merged by an editor). An editorial H2/H3 whose text exactly matches a
                       counter widget's own caption is labelled COUNTER CAPTION (a stat label, not a subheading
                       idea — the fix is demoting its tag, not merging it into other content). Everything else is
                       EDITORIAL. Duplicates: same level, identical after folding case and punctuation, or >90%
                       character similarity.
  Hidden containers .. 1 element the served HTML itself hides, excluded from the word count, headings inside them labelled HIDDEN and still counted:
                       <ul class="sub-menu"> — inline visibility:hidden — 4 words, 0 headings

## RENDERED-VS-SERVED DIFF
Text in HTML but invisible to humans ....... 6 elements by computed style  (1 call to action, 3 text, 1 statistic, 1 price)
    [call to action] “Get a Free Demo” — visibility:hidden (entrance-animation wrapper)
    [text] “Let Us Be Your Strategic Partner!” — visibility:hidden (entrance-animation wrapper)
    [statistic] “Annual Reportable Accidents Reduced By 31%” — visibility:hidden (entrance-animation wrapper)
    [price] “Hidden by display none $49 per month” — display:none
    [text] “Faded out heading text here” — opacity:0
    [text] “Offscreen promo text” — positioned off-screen
  Hidden by design, NOT findings ............ 5 elements  (1 screen-reader-only text, 2 in menus, 1 carousel slides waiting their turn, 1 in closed dialogs/popups)
  Served text blocks never appearing in visible text: 5
    “Get a Free Demo”
    “Let Us Be Your Strategic Partner!”
    “Annual Reportable Accidents Reduced By 31%”
    “Hidden by display none $49 per month”
    “Book a demo from the popup”
Text visible but absent from served HTML ... 2 lines (script-injected page content; invisible to any crawler that does not run JavaScript)
    “Cloned slide words that a loop carousel repeats for every testimonial shown twic”
    “Injected by JavaScript after load with a 22% figure”
  VOLATILE (third-party widget, may vary by time/session — not a page finding) 1 line this scan
    “Chat with EnVue Telematics”                                      seen 2/2 scans · from localhost · in #crisp-chatbox
    “We are online”                                                   seen 1/2 scans — absent this scan
    Detection: DOM insertions are attributed to the script host that made them; a body-level container named like a chat, cookie or consent widget also counts. Volatile lines never enter NEW / RESOLVED / WITHDRAWN; they carry a seen-in-N-of-M counter instead (tracking since September 8, 2026 at 3:39 AM UTC). This scan ran at September 8, 2026 at 3:39 AM UTC.
Entrance-animation markers ................. 2 elements carry one in served HTML (2 outermost blocks with text); in the browser: 2 markers at first paint → 1/2 released, 1 still hidden (“Get a Free Demo”) (1 still carrying the marker class)
                                             (an element never leaves the DOM when its animation fires — the marker class is removed)
Counter widgets ............................ 1 in served HTML; after render (1): “Annual Reportable Accidents Reduced By 3” shows 31 (hidden)

## CHANGES SINCE THE LAST SCAN (September 8, 2026 at 3:39 AM UTC)
  Page content ....... unchanged — same title, H1, meta, heading counts and text fingerprint
  Scanner rules ...... CHANGED since that scan (10.2 → 10.5). A finding that disappeared was re-verified item by item; none is marked RESOLVED on the rule change alone.
  Rank Score ......... 20 → 20  (inside the confidence band: sample noise, not movement)
  Answer Score ....... 15 → 15  (inside the confidence band: sample noise, not movement)
  Competitor sample .. 3 → 3 pages; median word count 558 → 558
  RESOLVED SINCE LAST SCAN ... none
  STILL PRESENT (re-verified) none
  NEW SINCE LAST SCAN ........ none
  UNCHANGED .................. 12 tasks carried forward
  WITHDRAWN .................. 
    “Remove 1 duplicated heading (1 distinct text repeated)” [Our Telematics Solutions Drive Results]
      why: page unchanged (same fingerprint), yet duplicate-heading check re-run on the fresh fetch: “Our Telematics Solutions Drive Results” now ×1. The scanner rules changed since that scan (10.2 → 10.5) and the re-run check is clean, so the earlier report was an artefact of the old rule, not something the page fixed.
  RESOLVED is claimed only when the exact check that produced the finding was re-run on this
  fetch and came back clean; “the page changed” is never evidence on its own. STILL PRESENT is a
  task that fell off the list (a rule or sample change) while its items are still on the page —
  it is carried into the tasks below, never closed. A WITHDRAWN task is one this tool reported
  last time and now believes was a false positive — the page did not change, so the finding
  could not have been fixed; it was a render or sample difference. If you acted on one, check
  that edit. A tool that silently dropped its own bad findings would teach you not to trust the
  good ones, so they are listed here instead.

## STEP 0 - VERIFY BEFORE YOU DO ANYTHING
This baseline came from an automated scan. Check it before you act on it.

1. Open EXACTLY this URL: http://127.0.0.1:41755/site/
   Not the homepage. Not a similar page. If the URL you can reach differs in any way, or you
   cannot open it at all, say so and stop.
2. Compare what you actually see against the MEASURED BASELINE below.
3. If ANY figure differs from the baseline, STOP. Do not start Task 1. List every difference
   as "baseline says X, I see Y" and ask me which is correct.
   Never write "confirmed" or "all accurate" beside numbers that do not match. A disagreement
   is a finding, not a formality — and it usually means one of us is looking at the wrong page.
4. The baseline figures are read from the served HTML; a rendered copy was also checked, so
   counter widgets and script-injected content are accounted for (see RENDERED-VS-SERVED DIFF).
   If you still find content on the live page that is missing from the baseline, flag it.
5. Content the browser check found hidden from visitors is listed above. If you see it on the
   live page with your own eyes, say so — a rendering difference between browsers is itself
   a finding worth recording.

## DO NOT MAKE THIS WORSE
- Do not change the URL.
- Do not edit the title tag or H1 without telling me the exact before/after and why — both carry
  ranking signal on their own, separate from anything else on this list.
- The ROLLBACK SNAPSHOT below is the restore point. Before any edit, confirm it still matches.

## ROLLBACK SNAPSHOT (as measured September 8, 2026 at 3:39 AM UTC)
Paste these back to undo. Everything here is the current live value, not a suggestion.

  Title tag .......... Render fixture
  Meta description ... (empty)
  H1 ................. Telematics solutions that drive results
  Canonical .......... (none)
  Heading outline .... 
    H1  Telematics solutions that drive results
      H2  Our Telematics Solutions Drive Results

## HOW I WANT YOU TO WORK
There are 12 tasks below in two explicitly ranked sections. Section A (OUT-OF-SCORE) is
visitor-facing and integrity defects that sit outside the two 100-point tables; each carries a
fixed weight, not table points. Section B (IN-SCORE) recovers points from the Rank and Answer
tables and is ordered by points per hour — arithmetic you can check. Ordering WITHIN each
section is arithmetic; ordering A before B is a judgement call, stated as such: a demo button a
visitor cannot see costs revenue now, a missing H3 does not. Work them in order. After each task,
show me the finished output and stop. Wait for me to say go before starting the next one.
Effort model: 1 day = 6 hours, half day = 3 hours, then 2 hrs / 1 hr / 30 min / 15 min. All pts/hr figures use
this. Section B labels are a pure function of pts/hr: HIGH ≥ 10, MEDIUM ≥ 5, otherwise LOW. CRITICAL is reserved
for Section A — something wrong on a live commercial page, not something merely absent.

ABSOLUTE RULE — DO NOT INVENT ANYTHING
Never make up a statistic, customer name, price, certification, award, product
capability, or comparison claim. If a task needs a fact you were not given, stop and
ask me for it. A plausible-sounding invented number on a commercial page is a legal
and reputational problem, not a helpful guess.

Before you start Task 1, ask me for two things: the current page copy, and the current
heading outline (or confirm the ROLLBACK SNAPSHOT outline is still current). Almost every
task below needs them.

## MEASURED BASELINE
From a live scan of the top 3 Google results for this keyword. SPREAD is min–max across them;
a median with a wide spread is a soft target, not a hard one.

  SIGNAL                        THIS PAGE  TOP-3 MEDIAN SPREAD (MIN–MAX)   MEDIAN RANGE (leave-one-out)
  ----------------------------------------------------------------------------------------------------
  Body word count             72 served · 48 rendered           558          558–558   stable   BASIS DIFFERENCE 33% (1 carousel clone slides (13 words) excluded on both sides)
  H2 sections                           1             3              3–3   stable
  H3 subheadings                        0             1              1–1   stable
  Question-shaped headings              0             2              2–2   stable
  Quantified claims                     2             5              5–5   stable
  Lists                                 0             1              1–1   stable
  Tables                                0        0 of 3                 
  Keyword in title tag                 NO        3 of 3                 
  Keyword in H1                       yes        3 of 3                 
  FAQ/Product/Service schema           NO        3 of 3                 

Word count follows the rule in METHODOLOGY (main content only, so a whole-page count reads
higher — whole page (served HTML) 79, whole page (rendered, painted) 55). The scored word count is the SERVED figure (72), the basis every page in the comparison shares; the rendered figure differs by 33% — see Word count check. Quantified claims are figures inside a sentence; a bare
number lifted out of a widget is not counted twice.

Current title tag: "Render fixture"
Current H1: "Telematics solutions that drive results"
Current meta description: "(empty)"
Schema, top-level entities (0): none
Schema, nested sub-objects (0): none

A note on targets: aim for PARITY with the median, never more. Overshooting a median is how
a page ends up padded with filler that dilutes it.

## CATEGORY VOCABULARY THIS PAGE IS MISSING
Concepts appearing across several pages that outrank this one, absent here. One line per
concept; surface forms in brackets. Presence was checked against the served HTML AND the
rendered visible text:
  PRESENT BUT HIDDEN = in the HTML, invisible to a visitor — scored as missing until visible
  ABSENT             = not on the page at all
  VOCABULARY
    • [ABSENT] Internet of Things  (also: IoT)  — on 3 of 3 ranking pages
    • [ABSENT] Driver Coaching  — on 3 of 3 ranking pages
    • [ABSENT] MPG  — on 3 of 3 ranking pages
    • [ABSENT] ELD  — on 3 of 3 ranking pages
  COMPETITOR
    • [ABSENT] Samsara-compatible  — on 3 of 3 ranking pages
OWN STACK = named elsewhere on this site (navigation, links, product pages) but not on this page
            — the business already offers it and the page never says so. Usually the best term here.
VOCABULARY = category language. Use judgement; I would rather you exclude a term than force it.
COMPETITOR = a domain ranking for this keyword. Do not add unless a real comparison exists.

## THE TASKS

### SECTION A — OUT-OF-SCORE (visitor-facing and integrity defects; fixed weight, ranked by weight per hour)

### Task 1 — Make 2 pieces of hidden content visible to visitors (1 call to action)
[CRITICAL · about 30 min · weight 45 outside the score tables, affects human visitors · ≈90 pts/hr]

Why: The page was rendered in a real browser with mouse and wheel input, scrolled, and left to settle; the render’s own self-check re-read every marked element by identity: 2 markers at first paint → 1/2 released, 1 still hidden (“Get a Free Demo”). Afterwards these elements still carried text that the computed style hides from every visitor while it sits readable in the served HTML. Most sit inside entrance-animation wrappers whose reveal never fired. A demo button, a price or a statistic a visitor cannot see costs revenue directly, which puts this above every content task. The diff lists 6 hidden content elements in total; this task covers the 2 that are calls to action, headings, statistics, prices or forms, and leaves aside 4 plain-text elements inside the same wrappers, which the same fix restores. 5 other hidden elements were excluded as hidden by design (menu items, carousel slides, closed dialogs, tabs, screen-reader text).

Measured:
  measured in browser → 1 call to action, 1 statistic  (+ 4 plain text, not in this task’s count)
  still hidden after render  → [call to action] Get a Free Demo — visibility:hidden
              [statistic] Annual Reportable Accidents Reduced By 31% — visibility:hidden

Do this: Give me the one-minute reproduction on the live page, then the diagnosis — stuck entrance animation, or a modal/tab that is meant to be closed — then the fix: page builder first, scoped CSS second, global override last with its cost stated. Anything hidden that is intentional, say so and move on.

### SECTION B — IN-SCORE (recovers Rank/Answer points; ranked by points per hour)

### Task 2 — Put the head term in the title tag
[HIGH · about 15 min · +20 pts on RANK + ANSWER · ≈100 pts/hr]

Why: 3 of 3 ranking pages lead with it. Yours does not contain “telematics” at all. Cheapest fix on this list.

Measured:
  now  → Render fixture
  set  → Telematics | 127

Do this: Write 5 title tags, 50-60 characters, "telematics" in the first 35 characters, ending " | 127". Show the character count for each and recommend one.

### Task 3 — Put the head term in the meta description
[HIGH · about 15 min · +5 pts on RANK · ≈25 pts/hr]

Why: Does not move rankings directly. It moves click-through, which does.

Measured:
  now → (empty)

Do this: Write 5 meta descriptions, 140-155 characters, containing "telematics", each with its character count.

### Task 4 — Mark up the FAQ and the products
[HIGH · about 1 hr · +15 pts on ANSWER · ≈15 pts/hr]

Why: Structured data makes content you already have machine-readable. Best result-to-effort ratio in the answer column — but only for content the page really carries.

Measured:
  found → no JSON-LD
  add   → FAQPage, Product or Service

Do this: Ask me for the FAQ content and product details, then produce only the JSON-LD the page content actually supports. If there is no FAQ on the page, say it has to be written first. If there are service categories rather than priced products, use Service and tell me what Product would need.

### Task 5 — Add scannable lists
[HIGH · about 1 hr · +10 pts on RANK · ≈10 pts/hr]

Why: Extractable structure, and it lifts dwell time.

Measured:
  you have → 0   median → 1

Do this: Find prose that should be lists and rewrite it as clean HTML lists with parallel grammar.

### Task 6 — Add a spec comparison table
[MEDIUM · about 2 hrs · +15 pts on ANSWER · ≈8 pts/hr]

Why: The most cleanly extractable structure on a page — an answer engine can lift a row without parsing prose, regardless of how many of the ranking pages happen to run one. Right now that is 0 of 3 — not yet the norm here, which is exactly why a clean table stands out rather than just matching the pack.

Do this: Ask me for the real specs, then build a comparison table as clean semantic HTML — rows are options, columns are dimensions, every cell a concrete value.

### Task 7 — Add H2 sections
[MEDIUM · about 1 hr · +7 pts on RANK · ≈7 pts/hr]

Why: Section count sits below the ranking set.

Measured:
  you have → 1   median → 3

Do this: Propose a revised H2 structure covering the buyer journey for "telematics".

### Task 8 — Add 2 question-shaped subheadings
[MEDIUM · about half day · +20 pts on RANK + ANSWER · ≈7 pts/hr]

Why: Each is a retrieval hook: the question a buyer types, answered in the 40 words directly beneath it. Also closes the word gap with substance instead of padding.

Measured:
  you have → 0   median → 2   spread → 2–2

Do this: Write 2 question headings, each followed by a 35-50 word answer that answers the question in its first sentence. Flag any figure you need from me rather than inventing one.

### Task 9 — Add 3 quantified, attributable outcomes
[LOW · about half day · +18 pts on ANSWER · ≈5 pts/hr]

Why: The highest-leverage change available. Answer engines quote figures they can attribute. With none on the page there is nothing to lift.

Measured:
  pattern  → [figure] [outcome] — [named customer], [timeframe]
  you have → 2   median → 5   spread → 5–5

Do this: Ask me for the real figures FIRST — customer, metric, timeframe, permission to name them — and suggest which 3 metrics would be most persuasive. Write nothing until I answer. If I have no data, give me a collection plan instead.

### Task 10 — Cover the category’s shared vocabulary
[LOW · about 2 hrs · +7 pts on ANSWER · ≈4 pts/hr]

Why: These concepts appear across multiple ranking pages — the language the topic is discussed in. A page missing them reads as off-topic to a retrieval model. 1 are competitor brands and are listed only so you know to leave them out.

Measured:
  vocabulary  → Internet of Things, Driver Coaching, MPG, ELD
  competitors → Samsara-compatible  (do not add)

Do this: Start with the OWN STACK terms — confirm each with me, then work them in. Then judge the VOCABULARY terms. Leave COMPETITOR terms out unless I say otherwise. Show before/after sentences for every term you add.

### Task 11 — Close the 486-word gap by answering real questions, not hitting a quota
[LOW · about 1 day · +13 pts on RANK · ≈2 pts/hr]

Why: Parity with the median, not maximum — and not a word count to write toward. A quota produces filler, which is the opposite of what closes a rank gap. List the buyer questions this page leaves unanswered, answer each one properly, and let the word count fall out of that. Both figures for this page: 72 served, 48 rendered (48 painted + 0 reachable). The gap is measured from the served figure because that is the basis every page in this comparison shares; the two bases differ by 33% on this page (1 carousel clone slides (13 words) excluded on both sides; 24 words served but not painted or reachable (script-gated, display:none, or inside a hidden template wrapper)), which is why the severity is capped.

Measured:
  you have → 72 served (scored) · 48 rendered   target → 558   gap → 486
  ranking pages run 558–558 words; the median is a soft target
  basis difference → 33% between served and rendered (1 carousel clone slides (13 words) excluded on both sides; 24 words served but not painted or reachable (script-gated, display:none, or inside a hidden template wrapper)); scored on served
  severity note → derived from a metric with a 33% basis difference (served vs rendered); capped at HIGH. Resolve the basis (render every page, or accept served) before committing a day to it.

Do this: Do not write to a word count. List the specific buyer questions about "telematics" this page leaves unanswered, using only the current copy plus facts I give you. Answer each one in as many words as it genuinely takes, then total the result — if it lands short of 558 words, tell me what real gaps remain rather than padding to close the gap.

### Task 12 — Build out H3 subheading depth
[LOW · about 1 day · +10 pts on RANK + ANSWER · ≈2 pts/hr]

Why: Subheading depth is how ranking pages carry detail without becoming a wall of text.

Measured:
  you have → 0   median → 1   add → 1

Do this: Produce a full heading outline taking the page to about 1 H3s, with a word budget beside each.

## SCORE RECONCILIATION
Every point below 100 on either table is either claimed by a task above or declared
unreachable here — so "nothing left to do" and "the tool ran out of ideas" never read the
same in this report.

  Rank score is 20.2/100 (79.8 points short of 100). 69.8 points claimed by a task above; 10 points declared unreachable this scan — no task exists for FAQ / Q&A block (0%, 10 pts short), each already at or above the threshold that generates a task, so the shortfall is fractional, not a missing idea.

  Answer score is 14.9/100 (85.1 points short of 100). 75.1 points claimed by a task above; 10 points declared unreachable this scan — no task exists for Observed answer-set coverage (0%, 10 pts short), each already at or above the threshold that generates a task, so the shortfall is fractional, not a missing idea.

## WHEN YOU ARE DONE
Give me a single checklist of every change, grouped by where it goes: title tag, head section,
page body, structured data. That is what I will hand to whoever edits the site.

Then list, in one place, every fact you needed from me and did not get.
