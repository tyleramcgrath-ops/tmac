// Thin entry point. The implementation is written to api/render.impl.js by build.js at build
// time from an immutable, hash-verified staging URL (see build.js).
module.exports = require('./render.impl.js');
