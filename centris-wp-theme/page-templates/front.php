<?php
/**
 * Template Name: Home (Full)
 *
 * Mirrors the single-page combined site — full hero, stats, AI pillars,
 * Sophia, workflow, services preview, industries preview, QA bundle,
 * why Centris, case studies, testimonials, and closing CTA.
 */

get_header();

include locate_template( 'template-parts/home-content.php' );

get_footer();
