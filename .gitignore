# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example
# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
.env*.local

# seo-intel engine local dev store (FileStore)
.data/

/test-results
/playwright-report
apps/reloop/package-lock.json
apps/north-star-hq/package-lock.json

# packaged theme and plugin builds — rebuildable from wp-content/, so the
# repo does not carry a 22MB zip per release
/palmtreesurf-*.zip
/palm-tree-bookings-*.zip
